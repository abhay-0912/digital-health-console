PORT=4000
JWT_SECRET=your_jwt_secret_here
# Database configuration
# If using Neon (Netlify DB) or any hosted Postgres set one of:
# NETLIFY_DATABASE_URL=postgres://user:password@host/dbname
# DATABASE_URL=postgres://user:password@host/dbname
# Leave both unset to fall back to local SQLite (database.sqlite)
# Neon automatically provides NETLIFY_DATABASE_URL in Netlify environment.

